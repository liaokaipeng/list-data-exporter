# 测试与回归

## 命令

```powershell
# 内容脚本语法检查（5 个文件，依赖序注入，见 docs/architecture.md）
Get-ChildItem extension/content/*.js | ForEach-Object { node --check $_.FullName }
node --check extension/background/service-worker.js

# 纯函数回归（util / detect / format，Node 直接运行）
node test/algo-check.cjs

# 生成扩展图标（改 gen-icon.ps1 后重跑，勿手改 PNG 二进制）
powershell -File test/gen-icon.ps1
```

## 浏览器回归（fixture.html）

1. `chrome://extensions` 刷新扩展 → 打开 `test/fixture.html`（本地文件需开启「允许访问文件网址」，或 `npx -y serve .` 后访问）
2. 点击扩展图标 → 面板应为**空态**「尚未收集任何列表」（无自动识别）
3. 对照 fixture.html 各区块注释的预期值逐项验证：

| 验证点 | 预期 |
| --- | --- |
| 空态 | 打开面板无任何列表，hint 引导「点击添加选择」 |
| 收集流程 | 「添加选择」→ 行出现「收集中」徽标 → 逐个点击商品卡片（点卡片内标题/正文均吸附整卡）→ 页面卡片显示序号框 1..6，面板计数与 hint「已收集 N 条」同步增长 |
| 拦截跳转 | 收集模式中点击「链接拦截验证」链接不跳转（收集其所在 p）；中键点击不新开标签；焦点在页面输入框按 Enter 不提交跳转；拖拽面板松开在页面上不误收集；Esc 结束后页面交互恢复 |
| li 吸附 | 收集模式下点击评论 li 内任意位置（含链接文字）→ 整个 li 成为一行 |
| 行内吸附 | 点击段落内行内 span → 吸附到整个 p；导出内容 = 整段归一化文本（`4722 PHP 币种已合并`） |
| 移除重排 | 收集模式中再次点击已收集元素 → 移除，序号徽标重排；移除至 0 条 → 列表取消选中 |
| 跨列表防护 | 第二次「添加选择」新建收集列表后，点击已属于第一个列表的元素 → toast「该元素已在其他收集中」 |
| 结束收集 | Esc / 「完成收集」→ 徽标消失，行保留且选中；未收集任何元素时 Esc → 整行移除回到空态 |
| 悬浮联动 | 悬浮面板条目 → 页面该列表全部元素紫色高亮框；悬浮页面已收集元素 → 面板条目滚入并短暂强调 |
| 选中切换 | 常规模式点击已收集元素 → 取消/恢复该列表选中（覆盖层消失/恢复）；贴屏幕边缘徽标翻内侧 |
| 附链接 | 评论列表勾选「附链接」→ 导出多一列（li 内第一个 a 的绝对地址） |
| 多列表导出 | 两个收集列表多选：xlsx 多 Sheet（收集1/收集2）；csv 拆多文件（带列表名后缀）；json 键嵌套；md 二级标题分区；html 多表串接 |
| 链接拦截 | 常规模式点「链接拦截验证」的链接 → 拦截 + 红框 + 警示 toast；收集模式点击 → 收集元素不跳转 |
| 交互放行 | 常规模式点击非收集区域（滚动、普通文本）不拦截 |
| 失联剔除 | 收集后触发页面替换 DOM（如翻页）→ toast「已收集元素被页面刷新移除」，空列表整行移除 |
| 图标再点 | 面板打开时点图标 → 面板收起（收集列表保留）；再点图标 / Esc /「退出」→ 全部清理 |
| 深色模式 | 系统切深色 → 面板/toast 跟随 |

## 已知不覆盖

- pickItem 吸附（依赖 getComputedStyle）与 UI 交互无自动化，依赖上表人工回归
- xlsx 二进制结构（Sheet 名 / 列宽钳制）以打开文件目测为准
